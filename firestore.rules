rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for Al-Jazeera & Euphrates Market as requested by the user
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
